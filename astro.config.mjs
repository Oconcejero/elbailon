// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Esta es la URL de navegación, no la de repositorio
  site: 'https://Oconcejero.github.io',

  // El nombre del repo está perfecto
  base: '/elbailon',

  trailingSlash: 'always',
});