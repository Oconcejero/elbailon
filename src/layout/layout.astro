---
import "../styles/global.css";
const { title = "Escuela de Danza El Bailón" } = Astro.props;
const base = "/elbailon";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    <header class="main-header">
      <div class="header-container">
        <a href={`${base}/`} class="logo">
          <img src={`${base}/EL_BAILON.png`} alt="Logo El Bailón" />
        </a>

        <button class="mobile-menu-toggle" aria-label="Abrir menú">
          <span class="bar"></span>
          <span class="bar"></span>
          <span class="bar"></span>
        </button>

        <nav class="nav-menu">
          <ul class="nav-links">
            <li><a href={`${base}/`}>Inicio</a></li>
            <li><a href={`${base}/actividades`}>Actividades</a></li>
            <li><a href={`${base}/horarios`}>Horarios</a></li>
            <li><a href={`${base}/fiestasyeventos`}>Fiestas y Eventos</a></li>
            <li><a href={`${base}/galeria`}>Galería</a></li>
            <li>
              <a href={`${base}/contacto`} class="btn-contacto">Contacto</a>
            </li>
          </ul>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>
  </body>

  <script>
    // Seleccionamos los elementos
    const menuToggle = document.querySelector(".mobile-menu-toggle");
    const navMenu = document.querySelector(".nav-menu");

    // Solo ejecutamos el código si AMBOS existen en la página
    if (menuToggle && navMenu) {
      menuToggle.addEventListener("click", () => {
        navMenu.classList.toggle("active");

        // Esto sirve para animar la hamburguesa si quieres
        menuToggle.classList.toggle("is-active");
      });
    }
  </script>
</html>
